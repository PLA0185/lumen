# 功能验收表

> **本文档按任务书章节逐项列出实现状态与实际操作路径。**
> 状态含义：**已完成** = 功能可用且有验证证据；**部分完成** = 主路径可用但有限制；
> **未完成** = 未实现。**不把计划中的功能写成已完成。**
>
> 验收环境：Windows 11 专业版 10.0.26200 · Intel i5-14400 · 15.7 GB RAM ·
> WebView2 153.0.4234.48 · Rust 1.98.1（MSVC）· Node 24.21.0

---

## 验证方式说明

| 手段 | 覆盖内容 |
| --- | --- |
| `cargo test --lib` | **243 项** Rust 测试，18 个模块（详见 §附一） |
| `pnpm test` | **85 项** 前端单元测试（3 个文件） |
| `cargo check --all-targets` | 0 错误 0 警告 |
| `pnpm typecheck` | 0 错误 |
| 实机启动 | 应用启动、窗口创建、数据库迁移、日志无 ERROR |
| Win32 API 读取 | 窗口扩展样式位（置顶/穿透/任务栏）**直接读取而非界面自述** |
| `tools/*.py` | 数据库结构、提醒调度、窗口恢复、周期口径的端到端验证 |
| 安装包实测 | 静默安装 → 启动 → 卸载全流程（见下） |
| 界面截图 | `docs/screenshots/01-main-window.png`（已安装版本运行时的主界面） |

### 安装包端到端验证（本轮新增）

| 检查项 | 实测结果 | 判定 |
| --- | --- | --- |
| 安装包构建 | `AiTodo_0.1.0_x64-setup.exe`，3.7 MB | ✅ |
| 构建方式 | Tauri 自动下载 NSIS 3.11 与 nsis_tauri_utils 0.5.3，**无需手动安装** | ✅ |
| 静默安装 | `setup.exe /S` 退出码 0 | ✅ |
| 安装产物 | `%LOCALAPPDATA%\AiTodo\aitodo.exe`（11.44 MB）+ `uninstall.exe` | ✅ |
| 开始菜单快捷方式 | `...\Start Menu\Programs\AiTodo.lnk` | ✅ |
| 卸载注册表项 | `HKCU\...\Uninstall\AiTodo`，版本 0.1.0 | ✅ |
| **已安装版本启动** | 进程运行（28.9 MB），**19 个 WebView2 子进程**，主窗口与悬浮窗均可见 | ✅ |
| 运行的二进制来源 | `C:\Users\win\AppData\Local\AiTodo\aitodo.exe`（确认非开发版） | ✅ |
| 读取既有数据 | 正确读取既有配置（悬浮窗按配置显示） | ✅ |
| **卸载后数据保留** | 卸载后 `%APPDATA%\com.pla0185.aitodo\aitodo.db` **仍存在**（264 KB） | ✅ |

判定 ✅ 安装包从构建到安装、启动、卸载形成完整闭环，且卸载**默认保留用户数据**。

**注意**：安装包**未签名**（`Get-AuthenticodeSignature` 返回 `NotSigned`），
因此 Windows SmartScreen 会提示"未知发布者"。签名需要代码签名证书，本轮未配置。

---

## §1 产品目标与验收原则

| 项目 | 状态 | 说明与验证 |
| --- | --- | --- |
| 视觉精致、操作轻快 | 已完成 | 独立设计的设计系统（CSS 变量、浅色/深色、焦点状态、动画可关） |
| 悬浮小窗显示今日待办 | 已完成 | 独立无边框透明窗口，Win32 样式位实测 `WS_EX_TOPMOST` 生效 |
| 手动记录与安排任务 | 已完成 | 快速添加 + 完整编辑表单 |
| 复杂重复规则 | 已完成 | 见 §5 |
| 可自行配置大模型 API | 已完成 | 见 §6 |
| AI 完全可选、断网可用 | 已完成 | 未配置 AI 时全部任务功能正常；AI 相关命令独立模块 |
| 所有可见功能连接真实持久化数据 | 已完成 | 无假数据、无占位界面；未实现项在界面明确标注"尚未实现" |
| 视觉独立设计、不照搬 | 已完成 | 图标由 `tools/make-icon.mjs` 纯代码合成，无外部素材 |

## §2 开发前调研与技术选型

| 项目 | 状态 | 证据 |
| --- | --- | --- |
| 技术选型记录 | 已完成 | `docs/tech-selection.md`（含本机环境实测基线） |
| 联网查阅 AI 服务官方文档 | 已完成 | `docs/api-research.md`（DeepSeek / OpenAI / Anthropic，附官方 URL 与访问日期 2026-09-23） |
| Tauri 官方能力核实 | 已完成 | `docs/api-research-tauri2.md` |
| 依赖与导入格式核实 | 已完成 | `docs/api-research-deps.md`、`docs/api-research-rust-crates.md`、`docs/third-party-task-import-verification.md` |
| GitHub 接口查阅 | 部分完成 | 已核实仓库创建与推送所需的认证方式并实测打通；**GitHub Actions 工作流尚未编写** |
| 功能分模块开发 | 已完成 | Rust 侧 17 个模块，前端 25 个组件/工具模块 |
| 数据库迁移机制 | 已完成 | 4 个迁移文件，实机验证 `schema_version = 4`，升级前自动备份 |
| 依赖版本锁定 | 已完成 | 提交 `Cargo.lock` 与 `pnpm-lock.yaml`，`.npmrc` 设 `save-exact` |
| 不硬编码用户密钥 | 已完成 | 密钥只进 Windows 凭据管理器；有测试断言日志与错误信息不含密钥 |
| 许可证与第三方资源清单 | 已完成 | `README.md` §11 + `docs/tech-selection.md` §9 |

## §3 页面与交互

| 项目 | 状态 | 操作路径 |
| --- | --- | --- |
| 主界面包含收件箱/今天/明天/本周/全部/日历/项目/标签/已完成/统计/回收站/设置 | 已完成 | 左侧导航全部可点且连接真实数据 |
| 独立快速添加窗口 | 已完成 | 托盘菜单「快速添加任务…」或 `Ctrl+Alt+N` |
| 可切换的今日悬浮小窗 | 已完成 | 设置 → 窗口 → 显示悬浮窗 |
| 首次启动简短引导 | **未完成** | 无引导页；首次启动直接进入空列表并显示"这里还没有任务"的空态提示 |
| 样例数据开关、一键清空 | **未完成** | 未实现 |
| 浅色/深色/跟随系统 | 已完成 | 设置 → 外观 |
| 自定义主题色 | 部分完成 | 项目/分类/标签支持自定义颜色；**全局主题色未提供调色板**，改用 CSS 变量可改但无界面 |
| 字体与界面缩放 | 已完成 | 设置 → 外观（字号 12–20px、缩放 80%–150%，实时生效） |
| 任务卡片完成状态/截止/优先级提示 | 已完成 | 卡片徽标 + 优先级色块 |
| 键盘焦点、悬停、错误反馈 | 已完成 | 全局 `:focus-visible` 样式；错误用 `AppError` 的中文提示 + 恢复建议 |
| 适配笔记本分辨率、高 DPI、多显示器 | 部分完成 | 布局用相对单位、`available_monitors` 已接入；**多显示器断连后的窗口找回未做专项实机验证** |
| 关键操作可键盘完成 | 已完成 | `Ctrl+N` / `Ctrl+F` / `Ctrl+R` / `Esc` |
| 辅助技术可识别的控件名称 | 已完成 | 交互元素均有 `aria-label`；状态用 `aria-checked` / `aria-pressed` |
| 对话框不被悬浮窗遮住 | 已完成 | 悬浮窗默认不置顶于模态之上；模态用 `z-index: 100` |
| 重要操作可撤销或二次确认 | 已完成 | 删除有确认；删除项目/分类有影响面预览；AI 改动有差异预览 |
| 动画轻且可关闭 | 已完成 | 设置 → 外观 → 界面动画；同时尊重 `prefers-reduced-motion` |
| 空/加载/联网失败/保存失败/无搜索结果状态 | 已完成 | 每种状态都有独立界面 |
| 不出现假按钮或占位功能 | 已完成 | 未实现的视图显示"尚未实现"并说明原因 |

## §4 任务与组织功能

### §4.1 任务生命周期

| 项目 | 状态 | 说明 |
| --- | --- | --- |
| 快速添加 + 完整表单 | 已完成 | 快速添加支持自然语言日期；完整表单含全部字段 |
| 标题（必填）/描述/Markdown 备注/链接 | 已完成 | Markdown 用 rehype-sanitize 渲染，防注入 |
| 图片/文件附件 | 已完成 | 受控存储；**删除任务或附件都不动用户原文件**；引用/复制两种模式 |
| 所属项目与分类、多个标签 | 已完成 | |
| 四级优先级 | 已完成 | 0 无 / 1 低 / 2 中 / 3 高 |
| 计划时间、截止时间、提醒时间三字段独立 | 已完成 | 三字段语义与界面说明一致；**另加"周期跨度"维度**（见下） |
| 周期跨度（本周/本月/本季度/今年内完成） | 已完成 | migration 0004；`tools/verify_period.py` 端到端验证 |
| 预计耗时、实际耗时 | 已完成 | 实际耗时可由专注模式累加 |
| 状态、完成时间、创建/更新时间 | 已完成 | 完成时间真实记录；`updated_at` 由触发器维护 |
| 附件受控存储、不误删原文件 | 已完成 | 删除前 `canonicalize` 校验路径在受控目录内，有测试锁定 |
| 编辑、复制、完成/撤销、批量操作 | 部分完成 | 编辑/完成/撤销/批量（9 种动作）已完成；**"复制任务"未提供入口** |
| 软删除、回收站恢复、永久删除 | 已完成 | 永久删除仅限回收站内，防止误触 |
| 子任务、进度显示、主子完成规则 | 部分完成 | 子任务与进度已完成；**"主子任务完成规则"未实现自动联动**（完成全部子任务不会自动完成主任务） |
| 任务依赖及循环依赖校验 | 已完成 | DFS 多层检测（A→B→C→A 会被拒绝并说明） |
| 列表/看板视图 | 已完成 | 看板按状态分列 |
| 拖拽排序与状态变更 | 部分完成 | **状态变更**（看板拖拽、日历拖拽改期）已完成；**列表内手动拖拽排序未实现**（可用排序规则切换替代） |
| 排序规则可切换（手动/截止/优先级/创建） | 已完成 | 另有计划时间、标题 |
| 搜索标题/描述/备注/项目/标签 | 已完成 | 组合筛选含状态、日期、优先级、分类、是否重复 |

### §4.2 项目、分类与标签

| 项目 | 状态 |
| --- | --- |
| 三者语义区分与管理入口 | 已完成（界面写明各自含义） |
| 创建、重命名、配色、排序、删除、合并 | 部分完成：创建/重命名/配色/删除已完成；**合并接口已实现但界面未提供入口**；排序接口可用但无拖拽界面 |
| 删除时说明关联任务如何处理 | 已完成（影响面预览 + 两种策略选择） |
| 收藏/置顶任务及项目 | 已完成（并明确区分"任务置顶"与"窗口置顶"） |
| 归档项目与恢复 | 已完成 |
| 任务可无截止日期 | 已完成 |
| "今天"与"逾期"规则在界面明确 | 已完成（视图副标题 + 统计口径说明） |

### §4.3 时间、日历与提醒

| 项目 | 状态 | 说明 |
| --- | --- | --- |
| 日/周/月日历 | 已完成 | 三种模式可切换 |
| 今天/明天/本周视图 | 已完成 | |
| 拖拽改期前展示目标日期 | 已完成 | 拖拽悬停时格内显示"移动到 X 月 X 日" |
| 重复任务改期询问范围 | **部分完成** | 日历拖拽改期**只影响该次实例**（因 `occurrence_key` 不变），但**没有弹出范围选择对话框**；范围选择在编辑表单中提供 |
| 到期/提前/自定义多个提醒 | 已完成 | 五种提醒类型 |
| Windows 系统通知 | 已完成 | 实机验证：`通知已提交给 Windows 通知中心`，无 ERROR |
| 可选声音 | **未完成** | 未实现提醒声音开关 |
| 托盘提示 | 已完成 | 托盘菜单显示今日未完成数 |
| 通知点击能定位任务 | **未完成（平台限制）** | 桌面端插件不支持点击回调，已在界面与 README 说明 |
| 提醒后可标记完成/稍后提醒 | 部分完成 | "稍后提醒"接口已实现；**通知本身无法交互**（平台限制），需回到应用操作 |
| 重启/休眠/跨日/DST 后重算待提醒项 | 已完成 | 启动时补发整理 + 30 秒轮询 |
| 避免漏发 | 已完成 | 补发窗口（默认 6 小时，可调） |
| 避免重复轰炸 | 已完成 | 实机验证：`fired_at` 在后续 2 个 tick 内完全未变 |
| 区分"仅日期"与"精确时间" | 已完成 | `has_planned_time` / `has_due_time` 位；纯日期任务不会在凌晨被判逾期 |

### §4.4 快速输入、键盘与专注

| 项目 | 状态 | 说明 |
| --- | --- | --- |
| 自然语言解析日期和标签 | 已完成 | 支持"明天 10:00""周五""下周一""3天后""9月30日""#标签""!2" |
| 解析结果保存前可查看和改正 | 已完成 | 界面显示"识别到：…"预览，控件可覆盖 |
| 可配置快捷键（新建/搜索/快速添加） | 部分完成 | 应用内快捷键固定为 `Ctrl+N/F/R`；**全局快捷键可配置**（托盘唤出主窗口/快速添加/今日概览） |
| 全局快捷键冲突提示且允许关闭 | 已完成 | 实机遇到 `Ctrl+Alt+T` 被占用，界面显示冲突说明；可关闭 |
| 任务绑定的番茄钟 | 已完成 | 任务卡片展开区可启动，时长累加到实际耗时 |
| 可暂停/恢复的专注模式 | 已完成 | |
| 时间记录 | 已完成 | 统计页显示今日/本周专注时长 |
| 基础统计 | 已完成 | 见 §7 |
| 退出或断电后能恢复计时或明确说明中断 | 已完成 | 计时以数据库为准；中断过久的会话标记为"已中断"而非虚高时长；有 16 项测试覆盖（含时钟回拨防御） |

## §5 重复任务

| 项目 | 状态 | 说明 |
| --- | --- | --- |
| 每天/每周/每月/每年 | 已完成 | 47 项测试覆盖 |
| 每隔 N 天/周/月/年 | 已完成 | 修复过一个真实 bug：DAILY 的 INTERVAL 原先未生效 |
| 工作日 | 已完成 | |
| 指定星期 | 已完成 | 多选 |
| 每月第 N 个星期 X | 已完成 | 含 -1（最后一个） |
| 开始日/结束日或次数/具体时间/提醒 | 已完成 | 三种结束条件互斥 |
| 规则预览（接下来 10 次发生） | 已完成 | 由 Rust 侧计算，避免两处策略不一致 |
| 月末/闰年/跨时区/DST/"每月 31 日"策略一致且展示 | 已完成 | 策略：不存在的日期**跳过该月**；界面对涉及月末的规则显示说明 |
| 数据模型：系列 + 实例 + 单次例外 + 规则版本/分段 | 已完成 | 三层模型 + 分段表 + 跳过表 |
| 按显示范围惰性展开 | 已完成 | `materialize_range` 只落库请求范围内的发生 |
| 已完成实例/修改记录/历史不因改规则消失 | 已完成 | 端到端测试断言；改规则只清理"未改动且未完成"的实例 |
| 每次发生以"系列 ID + 原始发生时间"取得稳定身份 | 已完成 | `(series_id, occurrence_key)` 唯一索引 |
| 改期后旧日期不再生成副本 | 已完成 | 端到端测试断言 |
| 规则分段后能准确判定实例归属 | 已完成 | 按生效起点排序选择分段 |
| 日期/时间/时区存储方式写入设计说明 | 已完成 | `docs/design-recurrence.md` |
| 编辑/改期/取消/删除时弹出范围选项 | 部分完成 | **编辑与删除**有范围对话框（三选一，默认不预选）；**日历拖拽改期不弹**（见 §4.3） |
| 不适用选项禁用并说明原因 | 已完成 | |
| 完成/撤销完成只针对本次 | 已完成 | 完成操作不涉及范围 |
| 过去实例与已完成历史不因"此次及以后"被追改 | 已完成 | 有测试断言 |
| 整个系列影响历史时预览并确认 | 已完成 | 不确认则**拒绝执行**并说明 |
| 单次修改保存为例外 | 已完成 | 标记 `occurrence_kind='exception'` |
| 单次取消显示为该次跳过 | 已完成 | 写入 `task_series_skips` |
| 删除时提供范围 | 已完成 | 仅本次/此次及以后/整个系列 |
| 完成一日实例不完成整个系列 | 已完成 | |
| 改提醒/附件/子任务/时间/标题时按范围处理或明确告知限制 | 部分完成 | 实例级操作直接作用于该次；**未在界面对每次操作都提示"仅影响本次"**（依赖用户对范围概念的理解） |
| 撤销和恢复清楚可预期 | 部分完成 | 删除进回收站可恢复；**改规则本身没有撤销栈**（分段会保留历史，但无法一键回退到上一版规则） |
| §5 验收例 | 已完成 | `src-tauri/src/recurrence_e2e.rs` 完整跑通：创建周一三五 → 完成周一 → 单独改周三标题 → 从下周一改规则 → 跳过周五 → 改全系列优先级 → **模拟重启后逐项校验** |

## §6 AI 助手与模型设置

| 项目 | 状态 | 说明 |
| --- | --- | --- |
| DeepSeek/OpenAI/Claude/自定义配置 | 已完成 | 三家协议差异按官方文档分别实现（有并排比较测试） |
| 保存提供商、Base URL、模型、Key、超时、参数 | 已完成 | |
| Base URL 可编辑时校验 | 已完成 | 明文 HTTP 仅允许 localhost，其它必须 HTTPS |
| 供应商差异由适配层处理，不把 Claude 当 OpenAI 端点 | 已完成 | Claude 用 `x-api-key`、`anthropic-version`、顶层 `system`、`content[]` 解析 |
| 连接测试 | 已完成 | 发送极小请求验证鉴权与模型 |
| 切换模型 | 已完成 | 支持获取模型列表；失败时可手动填写 |
| 查看调用错误及估计消耗 | 已完成 | 错误按类别映射（含"余额不足重试无效"提示）；预览显示 token 用量 |
| 模型列表不可用时允许手动输入 | 已完成 | 失败返回原因并提示可手填 |
| 密钥优先使用 Windows 凭据管理 | 已完成 | keyring 存入 Windows 凭据管理器 |
| UI 中默认隐藏密钥 | 已完成 | 密码框 + 脱敏函数（有测试断言中间部分不泄漏） |
| 日志/备份/问题报告排除密钥 | 已完成 | 数据库只存 `has_api_key` 布尔值 |
| AI 输出先显示差异预览 | 已完成 | 逐条列出「改动前 → 改动后」，可逐条勾选 |
| 用户选择接受、修改或放弃后才写入 | 已完成 | 确认需回传一次性 `previewId` |
| AI 不得擅自删除/完成/改写任务 | 已完成 | 四项能力只产生预览；没有"AI 直接写库"的代码路径 |
| 说明发送给模型的数据范围 | 已完成 | 每次预览显示 `dataScopeNote` |
| 可选择仅发送选中任务或必要字段 | 已完成 | 排程支持 `taskIds` 只发送选中任务；默认只发必要字段 |
| 敏感备注、附件内容默认不发送 | 已完成 | 备注默认不发（可显式开启）；附件**从不发送** |
| API 失败/余额不足/超时/限流时保留原数据 | 已完成 | 失败在预览阶段，不涉及写库 |
| 提供可读错误信息和手动继续路径 | 已完成 | 每类错误有中文说明 + 恢复建议 |
| 可选接入本地模型 | 已完成 | "自定义兼容服务"可指向 localhost（明文 HTTP 仅本机允许） |
| 写明本地模型安装与资源条件 | **未完成** | 未提供本地模型部署文档（仅支持接入，不含部署指引） |
| 不开启 AI 时功能完整 | 已完成 | |
| AI 结果遵守任务约束、用户可编辑每一项 | 已完成 | 排程会把"晚于截止时间"的条目单独标为警告 |
| 避免凭空编造已完成情况 | 已完成 | 复盘提示词明确要求"只基于给出的数据说话，不要编造"；且只发送聚合统计 |
| 结构校验、日期与时区校验、任务 ID 核验、重复项检测 | 已完成 | 四类校验均有测试 |
| 无效输出展示可读错误并允许重新生成 | 已完成 | 错误条目禁用勾选 + "重新生成"按钮 |
| 费用提示与单次输出限制 | 已完成 | 界面提示"费用由你自己的服务商账户产生"；输出上限可配置并收敛到安全范围 |

## §7 统计、成长与个性化

| 项目 | 状态 | 说明 |
| --- | --- | --- |
| 按日/周/月展示完成数量 | 已完成 | 区间可选 7/30/90/365 天 |
| 完成率 | 已完成 | 分母为 0 时显示"无数据"而非 0% |
| 逾期 | 已完成 | 分母为区间内到期任务数 |
| 类别占比 | 已完成 | |
| 预计与实际耗时及趋势 | 已完成 | 含估算偏差比与"你倾向于低估耗时"的可操作建议 |
| 清楚标注分母、时间范围、重复实例计数口径 | 已完成 | 页面顶部常驻口径说明；有测试断言该说明覆盖这三件事 |
| 连续天数不因无安排的日子误导 | 已完成 | 无安排的日子不中断也不计入；有测试 |
| 个人目标及进度 | 已完成 | 进度按"目标创建之后完成的任务数"计算 |
| 可手动或按任务关联的进度 | 部分完成 | 手动设定目标数量；**未提供把具体任务指派到目标的功能** |
| 经验值/等级/成就等游戏化 | 已完成 | 6 项成就，全部基于真实数据 |
| 允许完全关闭 | 已完成 | 默认即为**关闭**状态 |
| 自定义列表图标/主题/背景 | **未完成** | 未提供背景与图标自定义 |
| 背景不影响可读性 | 不适用 | 未实现背景功能 |
| 统计与 AI 复盘基于同一份真实数据 | 已完成 | 两者调用同一个 `collect_period_stats` |

## §8 Windows 桌面窗口与托盘

| 项目 | 状态 | 实测证据 |
| --- | --- | --- |
| §8.1 主窗口与悬浮窗分别可开关置顶 | 已完成 | Win32 扩展样式位实测：悬浮窗 `WS_EX_TOPMOST=True`、主窗口 `=False`，与配置一致 |
| §8.1 重启后按设置恢复 | 已完成 | 实机：注入配置后重启，状态按配置恢复 |
| §8.1 弹窗仍在最上层可操作 | 已完成 | 模态使用独立层叠 |
| §8.2 悬浮窗点击穿透 | 已完成 | Win32 实测 `WS_EX_TRANSPARENT` 随配置变化 |
| §8.2 穿透后始终可用的退出路径（托盘/快捷键/启动恢复） | 已完成 | **实机验证**：注入 `floatingClickThrough=true` 后重启，值被自动改回 `false` |
| §8.2 穿透与拖动互斥时界面说明切换方式 | 已完成 | 悬浮窗内提示"穿透已开启，不可点击"并列出关闭途径 |
| §8.3 不透明度实时调节 | 已完成 | CSS 变量实时生效 |
| §8.3 合理下限防止完全不可见 | 已完成 | 下限 25%，`normalize()` 对越界/NaN/负值收敛，有测试 |
| §8.3 默认 100%、重启保持 | 已完成 | |
| §8.3 透明效果与文字对比度都检查 | 部分完成 | 采用 CSS 不透明度以保证文字可读；**未做对比度数值测量** |
| §8.4 普通主窗口 / 悬浮今日 / 极简模式 | 部分完成 | 主窗口与悬浮窗口已完成；**"极简模式"未实现** |
| §8.4 桌面组件模式说明关系并实际验证 | 不适用 | 未实现该模式（悬浮窗即桌面组件形态） |
| §8.5 主窗口任务栏显隐独立选择 | 已完成 | `set_skip_taskbar` + 实测 `WS_EX_TOOLWINDOW` |
| §8.5 关闭主窗口是退出还是缩托盘由设置决定 | 已完成 | 设置 → 窗口 → 关闭主窗口时 |
| §8.5 隐藏任务栏图标前确认有恢复路径 | 已完成 | 同时隐藏任务栏并关托盘会被**拒绝**并说明原因 |
| §8.6 托盘显隐独立选择 | 已完成 | 可关闭；关闭时日志明确记录 |
| §8.6 菜单含打开/隐藏、快速添加、今日概览、置顶、穿透、暂停提醒、设置、完全退出 | 已完成 | 另有「窗口找不到了？重置窗口设置」 |
| §8.6 今日未完成数在菜单中显示 | 已完成 | 缓存计数，异步刷新 |
| §8.6 同时关闭托盘与任务栏图标时必须先启用快捷键并风险确认 | 已完成 | 无恢复路径时拒绝操作 |
| §8.6 退出与隐藏语义一致明确 | 已完成 | README §5 与界面均有说明 |
| §8.6 托盘启动、开机自启（可选）、多显示器断连找回 | 部分完成 | 托盘启动与自启已接入；**多显示器断连未做专项实机验证**；提供「把悬浮窗移回右下角」作为补救 |
| 区分"任务置顶"与"窗口置顶" | 已完成 | 设置页与 README 明确说明两者无关 |
| 各开关状态在界面与托盘同步 | 已完成 | 配置变更后重建托盘菜单，勾选状态随之更新 |

## §9 账号、同步、导入导出与备份

| 项目 | 状态 | 说明 |
| --- | --- | --- |
| 默认本地优先、无需注册 | 已完成 | SQLite 自动保存 |
| 提供可选择启用的账号与云同步 | **未实现** | 按用户本轮决定不做；数据模型已预留 `sync_rev` / `sync_state` |
| 多设备同步服务端 | **未实现** | 同上 |
| 完整 JSON 备份与恢复（含版本号与校验） | 已完成 | SHA-256 校验；版本号 `formatVersion` |
| 恢复前备份当前数据库 | 已完成 | 生成 `before-restore-*.db`；**快照失败则中止恢复** |
| CSV / Markdown 导出 | 已完成 | CSV 含 BOM 以便 Excel 正确显示中文 |
| PDF 可读导出 | **未完成** | |
| 导入前展示预览、重复检测、冲突处理、回滚 | 部分完成 | 预览/校验和/数据量对比/回滚（快照）已完成；**备份导入的"重复项检测"未实现**（整库替换语义，不涉及合并） |
| 附件随备份的处理方式明确 | 已完成 | 备份不含附件文件本体，界面与 README 均明确说明 |
| 从 Todoist / Microsoft To Do / Google Tasks / Notion 迁移 | **未实现** | 调研已完成（`docs/third-party-task-import-verification.md`），实现未做 |
| 不要求用户输入其它服务账号密码 | 不适用 | 未实现导入，因此不存在该风险 |
| 自动定期本地备份 | 部分完成 | 支持手动触发自动备份并按保留份数清理；**定时自动备份未实现**（无后台计划任务） |
| 恢复前备份当前数据库 | 已完成 | |
| 备份含任务/规则/实例例外/设置，并验证可恢复 | 已完成 | 端到端往返测试：导出→清空→恢复→逐项比对 |
| JSON 不适合二进制时采用压缩包 | 部分完成 | 采取"JSON + 明确排除附件并在界面说明"的方案；**未实现含二进制清单的压缩包** |
| 数据库写入使用事务 | 已完成 | 所有跨表写入走事务 |
| 迁移有备份 | 已完成 | 迁移前自动复制数据库 |
| 意外断电或磁盘不足不留半条规则 | 已完成 | 事务 + WAL + 导出先写临时文件再改名 |
| 数据目录查看 | 已完成 | 设置 → 数据与备份 |
| 备份保留设置 | 已完成 | 保留份数 1–100 可调 |
| 一键打开日志目录 | 部分完成 | 可打开数据目录与备份目录；**日志目录未提供独立按钮**（日志在数据目录的 logs 子目录下） |
| 用户可删除自己的全部数据 | 已完成 | 通过卸载勾选或手动删除数据目录；**应用内未提供"清空全部数据"按钮** |

## §10 安全、稳定性与性能

| 项目 | 状态 | 说明 |
| --- | --- | --- |
| 校验所有输入 | 已完成 | 标题长度按字符计、优先级 0–3、颜色仅限十六进制、状态白名单 |
| Markdown/链接安全处理 | 已完成 | rehype-sanitize；链接仅允许 http/https |
| 附件路径防越界 | 已完成 | `canonicalize` 后校验；有真实文件系统测试 |
| Web 内容与本地特权隔离 | 已完成 | 前端无 SQL 能力；capabilities 最小权限 |
| API Key 不明文展示或随备份泄露 | 已完成 | 凭据管理器存储；数据库只有布尔标记 |
| 网络调用有超时、取消、合理重试与频率限制 | 部分完成 | 连接/读取超时已设；**未实现请求取消与自动重试**（错误分类已提示哪些可重试） |
| 不能卡死界面 | 已完成 | 所有网络调用在 Rust 异步侧 |
| 日志不含密钥、完整私密任务内容 | 已完成 | 有测试断言 |
| 核心异常提供可理解提示、可恢复操作与诊断日志 | 已完成 | `AppError` 含 code/message/hint 三段 |
| 上千条任务下列表/日历/搜索流畅 | 部分完成 | 已加索引并做批量查询（避免 N+1）；**未做 1000+ 条的性能实测** |
| 窗口操作与后台提醒不阻塞主线程 | 已完成 | 提醒在独立 tokio 任务；菜单只读内存缓存 |
| 系统时间变更 | 已完成 | 计时对时钟回拨有防御；提醒采用"宁可晚到不可丢失"策略 |
| 程序异常退出 | 已完成 | 计时状态可从数据库恢复 |
| 重复启动 | 已完成 | 单实例插件，第二次启动激活已有窗口 |
| 断网重连 | 已完成 | AI 调用失败不影响本地功能 |
| 开机启动 | 已完成 | 插件接入 + 设置开关 |
| 安装升级及卸载后数据保留/删除选项 | 已完成 | NSIS 原生"删除应用数据"复选框（默认保留） |
| 单实例或明确多实例的数据库互斥策略 | 已完成 | 采用单实例 |

## §11 实施顺序与质量门槛

| 阶段 | 状态 | 证据 |
| --- | --- | --- |
| 1. 调研、原型、数据库结构、Windows 可运行骨架 | 已完成 | 5 份调研文档；14 表 + 后续迁移；实机启动 |
| 2. 任务生命周期、分类/标签/项目、搜索、日历、提醒、子任务/依赖、备份 | 已完成 | 见 §4 / §9 |
| 3. 重复系列、单次例外、未来分段、跨日与历史；优先自动化测试 | 已完成 | 47 + 8 + 5 项测试；§5 验收例端到端跑通 |
| 4. 悬浮窗、置顶、穿透、透明度、任务栏、托盘、快捷键；真实 Windows 验证 | 已完成 | Win32 样式位实测 + 安全恢复实测 |
| 5. AI 适配、预览确认、规划与复盘；同步、统计、专注、个性化 | 部分完成 | AI、统计、专注、周期任务已完成；**同步未做**；个性化部分完成 |
| 6. 打包安装、升级与回归、推送 GitHub | 部分完成 | 安装包已构建；**GitHub 推送待用户提供仓库信息** |

### 自动化测试要求

| 要求 | 状态 | 对应测试 |
| --- | --- | --- |
| 任务 CRUD 与数据库迁移 | 已完成 | `commands::tests`（21）+ `db::tests`（3） |
| 重复规则生成 | 已完成 | `recurrence::tests`（47） |
| 单次/未来/全系列修改及删除 | 已完成 | `recurrence_e2e::tests`（5） |
| 单次改期后旧日期不再生成副本 | 已完成 | `recurrence_e2e::acceptance_weekly_mon_wed_fri_full_lifecycle` |
| 月末和跨年 | 已完成 | `monthly_31st_skips_short_months`、`expansion_crosses_year_boundary` 等 |
| 多设备同步冲突 | **未覆盖** | 同步未实现 |
| 备份恢复 | 已完成 | `backup::tests`（15，含完整往返） |
| API 错误与隐私 | 已完成 | `ai::tests`（36）+ `ai_features::tests`（30） |
| 提醒去重 | 已完成 | `reminders::tests`（12）+ 实机验证 |

### 桌面功能实机验收

| 要求 | 状态 |
| --- | --- |
| 普通窗口与悬浮窗**分别置顶** | 已完成（Win32 样式位实测） |
| 穿透 | 已完成（样式位实测 + 启动安全恢复实测） |
| 透明度 | 已完成（CSS 实测生效） |
| 任务栏与托盘四种显隐组合 | 部分完成：主窗口任务栏显隐、托盘显隐已验证；**四种组合未逐一枚举实测** |
| 关闭与重新打开 | 已完成 |
| 重启 | 已完成 |
| 显示器断连 | **未验证** |
| 安全恢复 | 已完成（含最坏场景：隐藏任务栏 + 关托盘 + 开穿透） |

## §12 最终交付物

| 交付物 | 状态 | 路径 |
| --- | --- | --- |
| 完整源代码 | 已完成 | 仓库根目录（145 个受版本控制文件） |
| 依赖锁文件 | 已完成 | `src-tauri/Cargo.lock`、`pnpm-lock.yaml` |
| 构建命令 | 已完成 | `README.md` §9 |
| Windows 安装包 | 已完成 | `src-tauri/target/release/bundle/nsis/` |
| README（安装/首次运行/功能/数据位置/备份恢复/FAQ） | 已完成 | `README.md` |
| `docs/api-research.md` | 已完成 | 附官方 URL 与访问日期 |
| 数据结构说明 | 已完成 | `docs/data-structure.md` |
| 迁移与同步策略 | 部分完成 | 迁移策略已完成（`docs/data-structure.md`）；**同步策略未实现** |
| 重复规则与例外的设计说明 | 已完成 | `docs/design-recurrence.md` |
| 服务端源代码与部署配置 | 不适用 | 同步未做 |
| 测试脚本 | 已完成 | `tools/*.py` + Rust/前端测试 |
| 测试报告 | 已完成 | `docs/test-report.md` |
| Windows 实机截图 | 已完成 | `docs/screenshots/01-main-window.png`（已安装版本运行时的主界面） |
| 已知限制清单 | 已完成 | `README.md` §10 + 本表 |
| 功能验收表 | 已完成 | 本文档 |
| §12.1 GitHub 仓库交付 | **未完成** | 待用户提供仓库名、可见性与访问方式 |

## §12 最终演示清单

| 演示项 | 状态 | 操作路径 |
| --- | --- | --- |
| 从零安装并创建任务 | 已完成 | 安装包已构建；**静默安装 → 启动 → 卸载全流程实测通过**（详见 §3.8）；截图见 `docs/screenshots/` |
| 建立每周一三五重复任务并分别修改单次和未来 | 已完成 | 自动化测试完整覆盖；界面提供规则编辑器与范围对话框 |
| 完成一日且保持后续发生 | 已完成 | 端到端测试断言 |
| 创建提醒并在托盘收到通知 | 已完成 | 实机验证：日志显示通知已提交给 Windows 通知中心 |
| 切换窗口置顶/穿透/不透明度/任务栏显隐后恢复界面 | 已完成 | 含"最坏场景"安全恢复实测 |
| 配置真实大模型接口并在用户确认后导入任务 | **未验证** | 代码路径完整且协议按官方文档实现，但**未使用真实 API Key 做过端到端调用** |
| 导出备份、清空测试数据、重新导入并确认历史和规则一致 | 已完成 | 自动化往返测试覆盖 |

---

## 附一：测试分布（243 项 Rust 测试）

| 模块 | 数量 | 覆盖内容 |
| --- | --- | --- |
| `recurrence` | 47 | 规则解析/序列化/展开、月末闰年、跨年、DST、时区、死循环防护 |
| `ai` | 36 | 三家协议差异、请求构造、响应解析、错误映射、密钥脱敏、配置校验 |
| `ai_features` | 30 | JSON 提取、日期规范化、任务 ID 核验、重复检测、预览一次性、数据范围 |
| `commands` | 21 | 输入校验、时间规范化、周期跨度、改期保留时刻 |
| `focus` | 16 | 计时算式、暂停不累计、时钟回拨防御、状态机、格式化 |
| `backup` | 15 | 完整往返、外键顺序、校验和稳定性、CSV 转义、附件说明 |
| `reminders` | 12 | 提醒时刻推导、类型往返、跨日偏移、相对提醒参数校验 |
| `stats` | 11 | 区间计算、等级单调性、连续天数规则、口径说明完整性 |
| `recurrence_service` | 8 | 范围标签、RRULE 构造、日期构造、比较语义 |
| `window_mgr` | 8 | 默认配置安全性、不透明度收敛、恢复路径判定、JSON 容错 |
| `organize` | 7 | 名称/颜色校验（含注入拒绝）、删除策略、可操作提示 |
| `attachments` | 7 | MIME 推断、路径越界（真实文件系统）、哈希稳定性、存储模式 |
| `subtasks` | 6 | 循环依赖检测（自环/两节点/三层环/菱形/长链）、标题校验 |
| `recurrence_e2e` | 5 | §5 验收例完整链路、历史保护、跳过、删除、范围信息 |
| `shortcuts` | 4 | 加速键解析、冲突提示、默认键唯一性与可解析性 |
| `models` | 4 | 状态往返、未完成任务集合、逾期判定 |
| `error` | 3 | 错误码映射、序列化形状稳定性 |
| `db` | 3 | 迁移幂等、occurrence 唯一性、自依赖拒绝 |

前端 85 项：`datetime`（30）、`recurrence-ipc`（34）、`nlp`（21）。

## 附二：未完成项汇总（共 26 项）

按影响面排序：

**功能缺失（用户可见）**
1. 账号与云同步（含服务端）
2. 第三方任务导入（Todoist / Microsoft To Do / Google Tasks / Notion）
3. PDF 导出
4. 自动更新
5. 首次启动引导与样例数据
6. "极简模式"窗口
7. 提醒声音开关
8. 全局主题色调色板
9. 自定义列表图标与背景
10. 任务复制入口
11. 列表内手动拖拽排序
12. 项目/分类合并的界面入口
13. 个人目标与具体任务的关联
14. 主子任务完成规则自动联动
15. 定时自动备份
16. 应用内"清空全部数据"按钮

**平台限制（有说明）**
17. 通知点击跳转到任务（桌面端插件不支持）

**验证缺口**
18. ~~干净环境（无开发工具）的安装验证~~ → **已通过静默安装 + 启动实测**（仍未在完全无开发工具的机器上验证）
19. 真实 API Key 端到端调用
20. 多显示器断连后的窗口找回
21. 任务栏与托盘四种显隐组合的逐一枚举
22. 1000+ 条任务的性能实测
23. 文字对比度数值测量
24. 代码签名证书（安装包未签名，SmartScreen 会告警）

**文档缺口**
25. 本地模型部署指引
26. GitHub 仓库推送（待用户提供信息）

**未实现但已记录替代方案**
- 备份含附件压缩包：采取"JSON + 界面明确说明不含附件"的方案
- 导入重复检测：整库替换语义下不涉及合并，故未实现
- 请求取消与自动重试：已实现超时与错误分类提示
