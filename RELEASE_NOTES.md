# Lumen 0.2.2

## 悬浮窗加了显眼的开关

你反馈"悬浮窗在哪开启，我都找不到"——原来只有两个入口，都不在主界面上：

- 「设置 → 窗口 → 显示悬浮窗」
- 托盘图标右键菜单 →「显示悬浮窗」

现在**主界面顶栏左侧多了一个「悬浮窗」按钮**（图钉图标），点一下即开、
再点即关，开启时按钮高亮。打开后会提示它出现在哪：

> 悬浮窗已打开：在屏幕右下角，可拖动、可改大小与透明度，鼠标穿透在它自己的顶栏切换

三个入口都保留，任选其一即可：顶栏按钮、设置页、托盘菜单。

另外顶栏按钮与设置页/托盘的改动是**双向同步**的：从托盘关掉悬浮窗，
顶栏按钮也会跟着变回未选中状态。

---

# Lumen 0.2.1

## 换了一整套统一图标

初版侧边栏用的是 `☀ ⛅ 🗓 📅 ▦ 📥 📋 ⑦ ㊳ ◲ ❶ 📁 🏷 📊 ⚙` 这类字符，问题是它们
**根本不是一个体系**：彩色 emoji 与纯文字符号混排，粗细、大小、基线全对不齐，
配色也不受控（emoji 自带颜色，和深浅主题打架），换个系统还会长得完全两样。

现在改成**自绘 SVG 图标集**（`src/components/Icons.tsx`，40 个图标）：

- 统一 24×24 视口、线宽 1.7、圆角端点，并排看不会有的重有的轻；
- 全部使用 `currentColor`，因此浅色/深色主题、选中态、悬停态、禁用态自动适配；
- 周/月/季/年四个周期视图共用同一个外框、只在框内做区分，一眼能看出是一族；
- 附件类型（图片/文档/音频/视频/压缩包）也换成同一套"纸张"外形；
- 顺手修掉了侧边栏左上角还留着改名前的 **A** 字母——现在是"光"的意象标记。

图标全部是手写坐标，没有引入任何图标库，符合任务书"视觉独立设计、不照搬"的要求。

## 验证方式

用 WebView2 的调试协议直接抓真实渲染结果（`tools/screenshot.py`），
而不是靠"应该没问题"：截图见 `docs/screenshots/ui-icons-main.png`
与 `ui-icons-floating.png`。

---

# Lumen 0.2.0

本轮把「用起来更省事」的几件事补齐了，并且**从现在开始，以后升级不用再自己下载安装包**。

## 新增

- **自动更新**：应用内「设置 → 关于 → 软件更新」可检查、下载并安装新版本；安装包用 minisign 私钥签名，客户端用内置公钥校验，**校验不通过直接拒绝安装**。启动 20 秒后也会静默检查一次，发现新版本只提示、不擅自安装。
- **PDF 导出**：把当前列表（沿用你此刻的筛选与搜索条件）导出成一份可读的 PDF，含标题、生成时间、统计概览，并按「已逾期 / 进行中 / 已完成」分组，逾期行标红。**中文由系统字体渲染，不存在乱码或方块字**。
- **拖拽排序**：在「手动排序」且未搜索时，直接拖动任务卡片调整顺序（卡片左侧有拖拽手柄）。用中点插入法只更新被拖动的那一行，上千条任务也不卡。
- **任务复制**：卡片上的 ⧉ 一键复制为副本，标题自动变成「（副本）」「（副本 2）…」。副本状态重置为未完成、子任务进度重置，标签/子任务/提醒一并复制；**附件不会复制**，界面会明确告诉你多少个附件需要重新添加。
- **项目 / 分类 / 标签合并**：整理页每一项都新增「⇥ 合并」，可把重复项合并到一个保留项，任务归属自动转移，源项软删除（可以从数据库追溯，不是直接抹掉）。
- **悬浮窗增强**：
  - 顶栏新增 **置顶开关**（📌，开启时高亮）与 **鼠标穿透** 开关，不用再跑到设置页；
  - **修掉了一个真实缺陷**：顶栏既是拖动区又放着按钮，按下按钮时先触发了窗口拖动，`click` 永远不会发生——表现为"置顶/穿透/关闭点了没反应"。现在只有从顶栏空白处、底栏等非交互区域起拖才会移动窗口；
  - **不透明度可在悬浮窗内直接拖动调节**（底部滑块，25%–100%），拖动即时生效、松手后记住；
  - **大小可拖动右下角把手调节**，松手后自动记住；尺寸有上下限保护，拖到极小或极大都会被收敛，不会出现内容溢出或超出屏幕；
  - **悬浮窗内可以直接完整编辑任务**：点任务右侧 ✎ 打开的是与主窗口**同一个编辑表单**（描述、备注、链接、优先级、项目/分类、计划与截止时间、子任务、附件、提醒、依赖），不再是只能改标题或加任务；
  - **双击任务标题即可就地改名**，回车保存、Esc 取消；
  - 底部新增输入框，**直接在悬浮窗里添加今天的任务**，不跳窗口。
- **提醒可点击跳转**：提醒触发时，应用内会弹出一条**不会自动消失**的提醒条，点「打开任务」直接打开对应任务。桌面端系统通知没有点击回调（需要应用有打包身份），这是可用的替代路径。
- **跨窗口实时同步**：在悬浮窗里勾选或改名，主窗口立刻跟着变；反之亦然。以前要等下一次轮询（最多 30 秒）。

## 修复

- **编辑任务保存不了（最严重的一个）**：SQL 拼装时 `Separated::push()` 本身就会写入分隔符，`sep.push("title = ").push_bind(v)` 生成的是 `title = , ?1`，SQLite 直接报 `near ",": syntax error`。这个写法在三个文件里出现了 52 次，意味着**任务、项目、分类、标签、子任务的"编辑/保存"全部会失败**。之前只有校验函数的单元测试，没有真的执行过 UPDATE，所以一直没暴露。现已全部修正，并补上会真实执行 UPDATE 的回归测试。
- **全新安装后创建第一条任务会失败**：`sort_order` 是 REAL 列，空表时 `COALESCE(MAX(sort_order), 0) + 1` 的求值结果被 SQLite 判成 INTEGER，驱动按浮点解码即报类型不匹配。这个缺陷只在空库上出现。
- **拖拽排序在极深位置插入时会把任务插到列表最前面**：中点插入法触发「重新编号」后仍使用旧坐标计算位置。现在重新编号后会重新取邻居，两次仍分不开则明确报错而不是乱插。
- **悬浮窗顶栏按钮点了没反应**（见上文「悬浮窗增强」第一条）。
- **导出的 PDF 是一张空白页**：打印报告被渲染在 `.app` 容器内部，而导出时正是用 `body[data-print='on'] .app { display:none }` 隐藏主界面，报告跟着一起被隐藏——WebView2 打印"当前页面"，结果自然什么都没有。现在报告挂在 `.app` 之外；修好前后同一份数据的导出分别是 1 KB（空白）和 90 KB（完整）。
- 悬浮窗此前无法收到任何窗口级权限（能力集只覆盖了主窗口），拖动、置顶切换、事件监听都会被拒绝；现在为悬浮窗与快速添加窗单独配置了最小能力集。
- 新增配置项时，老的配置文件不会因为缺少新键而整体回落到默认值（容器级 `serde(default)`），避免用户调好的设置被一次性清掉。

## 验证

- Rust 测试 **257 项**、前端 **85 项**，全部通过；`cargo check --all-targets` 0 错误 0 警告；`tsc --noEmit` 0 错误。
- 新增 `commands_e2e` 集成测试，覆盖复制、排序、合并、**以及"真的执行一次更新"**：回收站任务不受合并影响、连续复制产生可区分名字、反复插入同一位置不丢任务、单字段/多字段/清空字段/完成与撤销都能真正写库。
- 实机验收脚本（通过 WebView2 的 CDP 调试协议驱动真实界面，而不是截图猜坐标）：
  - `tools/verify_v020.py`：新建、复制、拖拽排序、悬浮窗显示与就地编辑；
  - `tools/verify_floating.py`：**用真实鼠标事件**点击悬浮窗按钮、拖动不透明度、改变尺寸并检查是否有溢出等显示问题、在悬浮窗里打开完整编辑并真正保存。

## 安装说明

- 安装包未做代码签名，Windows SmartScreen 会提示"未知发布者"，点"仍要运行"即可。
- 数据存放在 `%APPDATA%\com.pla0185.lumen`，升级只替换程序文件，**不会动你的任务数据**。
- 卸载时可以自己决定是否一并删除应用数据。

## 自动更新的资产说明

- `Lumen_0.2.0_x64-setup.exe` —— 手动安装用，同时也是**自动更新的更新包本体**（Tauri v2 在 Windows 上直接用 NSIS 安装器做更新包，不再单独打 zip）。
- `Lumen_0.2.0_x64-setup.exe.sig` —— 该安装包的 minisign 签名，客户端下载后会用它校验，校验不过直接拒绝安装。
- `latest.json` —— 更新清单，客户端启动后从这里读版本号、下载地址与签名。
